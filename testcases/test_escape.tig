let 
    var a:int := 1  /* a.escape = True */

    function fun1() = 
        let
            var a: int := a + 1 /* a.escape = False */
            var b: int := 2  /* b.escape = True */

            function fun2(a: int) =  /* a.escape = False */
                if b <> 2
                then 
                    print("x")
                else
                    print("y")
        in
            fun2(a)
        end
in
    fun1()
end
